name: bufferwave_core
description: BufferWave Core — Moteur réseau VPN pur. API-only, sans interface.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^3.0.0
  http: ^1.2.0
  shared_preferences: ^2.2.0
  connectivity_plus: ^5.0.2
